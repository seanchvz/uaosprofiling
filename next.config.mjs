/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: ["localhost", "https://img.clerk.com", "img.clerk.com"], // tawagon kung asa gikan ang pic
    }
};

export default nextConfig;
